import { Logger.Middleware.TsMiddleware } from './logger.middleware.ts.middleware';

describe('Logger.Middleware.TsMiddleware', () => {
  it('should be defined', () => {
    expect(new Logger.Middleware.TsMiddleware()).toBeDefined();
  });
});
